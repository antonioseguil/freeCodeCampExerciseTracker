const User = require("./../models/user");

const allItems = (req, res) => {
  res.send({ a: 1 });
};

const findItem = () => {};

const createItem = () => {};

module.exports = {
  allItems,
  findItem,
  createItem,
};
